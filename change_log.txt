Only for keeping track until the next commit.

1. Variable delta-time flag is now determined by the program, it cannot be set in settings.ini.
2. -min CL argument implemented.
3. tick module now loads only when not running in minimal mode
4. Enchanced the status report.
5. -fe CL argument implemented.
6. -t has split into -tu (time universe) and -tr (time real).
7. Confirmed that continuation of updates is possible when the standard saving scheme is in effect (to the original or a copy).
8. Implemented the continuation of separate save file save scheme unis via the -cnt flag.
9. Implemented the -se flag to save the simulation when it's over.
10. General bugfixes.

